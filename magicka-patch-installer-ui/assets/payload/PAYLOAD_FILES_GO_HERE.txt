Release builds now sync the patched payload files from the Steam Magicka folder into
the repository root:

  Magicka.exe
  PolygonHead.dll

The Windows Flutter build copies those repo-root files next to the generated installer
EXE. This folder remains as a development fallback only; the installer can still read
payload files from assets\payload if they are placed here manually.
